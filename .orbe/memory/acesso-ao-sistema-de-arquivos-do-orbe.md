# Acesso ao Sistema de Arquivos do Orbe

> Memória do orbe — tópico `acesso-ao-sistema-de-arquivos-do-orbe` (auto-gerada, filtrada de segredos).

## 2026-07-05
O orbe (assistente) tem acesso restrito ao sistema de arquivos, podendo criar e manipular arquivos apenas dentro da pasta raiz do projeto atual, e não em diretórios externos como `/tmp/`.
